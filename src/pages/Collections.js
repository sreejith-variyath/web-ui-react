import React from 'react';
import CollectionList from '../containers/CollectionListContainer';

var collections = React.createClass({
	render:function() {
		return <CollectionList />
	}
});

export default collections;